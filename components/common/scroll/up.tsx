import { FC } from "react";
import _ from "lodash";
import { jsonParse } from "@/utils/helper";

type PropsType = {
  options?: any;
};

const Up: FC<PropsType> = ({ options }) => {
  return <>scroll up</>;
};

export default Up;
